########################DEPA 2#########################
################ALEJANDRO MU?OZ AHUMADA################

#Los emails son: diana.lopez@utem.cl y amaru.fernandezd@utem.cl
#Asunto: BD2_ALEJANDRO_MU?OZ.



########################Pregunta 1(10 ptos)########################


#Usted tiene $50.000 pesos en una cuenta de ahorro,de los cuales debe invertir la totalidad en una acci?n de la empresas Data Fae,
#para esto debe crear un algoritmo que le permita decidir si compra,
#vende o hace nada seg?n la estrategia que usted establezca dado el comportamiento de los precios de esta acci?n,
#considerando que el rango de an?lisis es de 100 d?as, la funci?n debe entregar la ganancia obtenida durante este periodo.
#Por ejemplo, usted podr?a considerar promediar los precios de los ?ltimos tres d?as, dado su posici?n en el rango establecido,
#para comprar la acci?n. Luego considerar que la rentabilidad de esa acci?n que tiene supere cierto umbral para volver a venderla,
#y as? sucesivamente

#Utilice for, if else, sample, vectores, entre otras herramientas que se pasaron en clases.

#Primeros cuatro d?as del comportamiento de la acci?n
#vectorValorAccion <- sample(1000:3000,4)
# Establecer limite de parada en n=100
n = 100

# Vector inicial
t <- sample(1000:3000,4)

# ciclo desde 5 hasta 100 (dias)
for (i in 5:n) {
  
t <-c (t, sample(1000:3000,1) )
# Calcular el tama?o del vector

tamanio <- length(t)
# Seleccionar los tres ultimos valores, se promedia, y hacer la comparacion para decidir que hacer: Invertir, Vender, Nada.

# Si el promedio es mayor al ultimo valor: vendemos
# Si el promedio es menor al ultimo valor: invertimos
# Si el promedio es igual al ultimo valor: no hacemos nada
  
promedio <- (t[tamanio] + t[tamanio-1] + t[tamanio-2])/3
  
if(promedio > t[tamanio]) {
print("Vender")
} 
else if(promedio > t[tamanio]) {
"Invertir"
}
else {
print("No hacer nada")
}
}

ahorro <- 50000
beneficio <- ahorro+promedio




########################Pregunta 2 (5 ptos)#############################


#Elabore una funci?n incorporando "n" datos
#calcule, media, mediana, m?ximo y m?nimo.
#Debe utilizar solo for, if, else.

# Introducir el valor de n
n <- 10
  
# Crear el vector
vector_n <- c(1,20,3,40,5,60,7,80,9,100)
  
# Generando una lista con limite de n
for (i in 1:n) {
vector_n <- c(vector_n, i)
}
  
if(n > 0) {
message("Media: ")
print(mean(vector_n))
message("Mediana: ")
print(median(vector_n))
message("Maximo: ")
print(max(vector_n))
message("Minimo: ")
print(min(vector_n))
}    else {
print("Entrar un valor de n positivo")
}
  









